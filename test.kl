using io;

int main() {

    io::out("for with no init:\n");
    int x = 0;
    for (; x < 3; x += 1) {
        io::out("{}\n", x);
    }

    io::out("for with no step:\n");
    for (int y = 0; y < 3;) {
        io::out("{}\n", y);
        y += 1;
    }

    return 0;
}
